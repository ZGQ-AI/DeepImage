server:
  port: 8080

spring:
  application:
    name: DeepImage
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/deepimage
    username: postgres
    password: postgres